apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-chatbot.fullname" . }}
  labels:
    {{- include "ai-chatbot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ai-chatbot.selectorLabels" . | nindent 4 }}
