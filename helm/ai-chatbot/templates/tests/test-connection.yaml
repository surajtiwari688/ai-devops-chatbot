apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ai-chatbot.fullname" . }}-test-connection"
  labels:
    {{- include "ai-chatbot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ai-chatbot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
